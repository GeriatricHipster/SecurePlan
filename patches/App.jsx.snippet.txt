# App.jsx patch notes

1. Import the new components:
   import ThemeToggle from './components/ThemeToggle.jsx';
   import FullScreenToggle from './components/FullScreenToggle.jsx';

2. Render the toggles in the app shell so they appear on all pages:
   <>
     <ThemeToggle theme={theme} onToggle={toggleTheme} />
     <FullScreenToggle />
     <div className={`app-shell app-shell--${route.page}`}>
       ...
     </div>
   </>

3. Rename the Sites page label to Library:
   - navigation button text
   - route/page title
   - page heading

4. If you are adding the measurement tool:
   import MeasurementTool from './components/MeasurementTool.jsx';
   Then wire it into the survey editor with:
   - measure mode state
   - calibration state
   - a click handler that captures two points
   - a red SVG line overlay
   - a scale conversion from pixels to feet
